<h1>Working!</h1>

<div class="listings">
    <% for listing in @listings %>
        <li><%= listing.title %></li>
        <li><%= listing.message %></li>
    <% end %>

    <span>Listing count: <%= @listings.count %></span>
</div>